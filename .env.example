# Uygulama
PORT=5000
NODE_ENV=development
JWT_SECRET=your_super_secret_jwt_key_here
JWT_EXPIRE=7d

# Yerel geliştirme (npm run dev)
MONGO_URI=mongodb://localhost:27017/portfolyo

# Docker (docker-compose up) — MONGO_URI otomatik ayarlanır,
# sadece aşağıdaki değişkeni set et:
MONGO_DB=portfolyo
